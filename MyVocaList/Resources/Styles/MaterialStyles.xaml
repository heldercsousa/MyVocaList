<?xml version="1.0" encoding="UTF-8" ?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:dx="http://schemas.devexpress.com/maui">

    <!-- ================================================================ -->
    <!-- CONTAINER STYLES                                                 -->
    <!-- ================================================================ -->

    <Style x:Key="PageContainer" TargetType="VerticalStackLayout">
        <Setter Property="Padding" Value="16" />
        <Setter Property="Spacing" Value="16" />
    </Style>

    <Style x:Key="SectionContainer" TargetType="VerticalStackLayout">
        <Setter Property="Spacing" Value="12" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0,0,0,24" />
    </Style>

    <!-- ================================================================ -->
    <!-- DEVEXPRESS BUTTON STYLES (MD3-Inspired)                          -->
    <!-- ================================================================ -->

    <Style TargetType="dx:DXButton" x:Key="FilledButton">
        <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
        <Setter Property="TextColor" Value="{StaticResource OnPrimary}" />
        <Setter Property="CornerRadius" Value="20" />
        <Setter Property="HeightRequest" Value="40" />
        <Setter Property="PressedBackgroundColor" Value="{StaticResource Primary}" />
        <Setter Property="PressedTextColor" Value="{StaticResource OnPrimary}" />
    </Style>

    <Style TargetType="dx:DXButton" x:Key="FilledTonalButton">
        <Setter Property="BackgroundColor" Value="{StaticResource SecondaryContainer}" />
        <Setter Property="TextColor" Value="{StaticResource OnSecondaryContainer}" />
        <Setter Property="CornerRadius" Value="20" />
        <Setter Property="HeightRequest" Value="40" />
    </Style>

    <Style TargetType="dx:DXButton" x:Key="OutlinedButton">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="TextColor" Value="{StaticResource Primary}" />
        <Setter Property="BorderColor" Value="{StaticResource Outline}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="20" />
        <Setter Property="HeightRequest" Value="40" />
    </Style>

    <Style TargetType="dx:DXButton" x:Key="TextButton">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="TextColor" Value="{StaticResource Primary}" />
        <Setter Property="CornerRadius" Value="20" />
        <Setter Property="HeightRequest" Value="40" />
    </Style>

    <!-- ================================================================ -->
    <!-- DEVEXPRESS EDITOR STYLES                                         -->
    <!-- ================================================================ -->

    <Style TargetType="dx:TextEdit">
        <Setter Property="LabelText" Value="" />
        <Setter Property="BoxMode" Value="Outlined" />
        <!-- BoxCornerRadius property removed in DevExpress 25.1.3+ -->
        <!--<Setter Property="BoxCornerRadius" Value="4" />-->
        <Setter Property="FocusedBorderColor" Value="{StaticResource Primary}" />
        <Setter Property="BorderColor" Value="{StaticResource Outline}" />
        <Setter Property="BackgroundColor" Value="{StaticResource SurfaceContainerHighest}" />
        <Setter Property="TextColor" Value="{StaticResource OnSurface}" />
    </Style>

    <!-- ================================================================ -->
    <!-- FLYOUT MENU STYLES                                               -->
    <!-- ================================================================ -->

    <Style TargetType="dx:DXButton" x:Key="FlyoutMenuButton">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="TextColor" Value="{StaticResource OnSurfaceVariant}" />
        <Setter Property="PressedBackgroundColor" Value="{StaticResource SurfaceContainerHigh}" />
        <Setter Property="Padding" Value="16,12" />
        <Setter Property="CornerRadius" Value="28" />
        <Setter Property="HeightRequest" Value="48" />
        <Setter Property="HorizontalContentAlignment" Value="Start" />
        <Setter Property="IconColor" Value="{StaticResource OnSurfaceVariant}" />
        <Setter Property="FontFamily" Value="RobotoMedium" />
        <Setter Property="FontSize" Value="14" />
    </Style>

    <!-- ================================================================ -->
    <!-- SHELL & NAVIGATION                                               -->
    <!-- ================================================================ -->

    <Style TargetType="Shell" ApplyToDerivedTypes="True">
        <Setter Property="Shell.BackgroundColor" Value="{StaticResource Surface}" />
        <Setter Property="Shell.ForegroundColor" Value="{StaticResource OnSurface}" />
        <Setter Property="Shell.TitleColor" Value="{StaticResource OnSurface}" />
        <Setter Property="Shell.NavBarHasShadow" Value="False" />
        <Setter Property="Shell.TabBarBackgroundColor" Value="{StaticResource SurfaceContainer}" />
        <Setter Property="Shell.TabBarForegroundColor" Value="{StaticResource Primary}" />
        <Setter Property="Shell.TabBarUnselectedColor" Value="{StaticResource OnSurfaceVariant}" />
    </Style>

    <!-- ================================================================ -->
    <!-- TYPOGRAPHY (MD3 Roboto)                                          -->
    <!-- ================================================================ -->

    <Style TargetType="Label" Class="Display.Large">
        <Setter Property="FontFamily" Value="RobotoRegular" />
        <Setter Property="FontSize" Value="57" />
    </Style>

    <Style TargetType="Label" Class="Headline.Large">
        <Setter Property="FontFamily" Value="RobotoRegular" />
        <Setter Property="FontSize" Value="32" />
    </Style>

    <Style TargetType="Label" Class="Title.Medium">
        <Setter Property="FontFamily" Value="RobotoMedium" />
        <Setter Property="FontSize" Value="16" />
    </Style>

    <Style TargetType="Label" Class="Body.Medium">
        <Setter Property="FontFamily" Value="RobotoRegular" />
        <Setter Property="FontSize" Value="14" />
    </Style>

    <Style TargetType="Label" Class="Label.Large">
        <Setter Property="FontFamily" Value="RobotoMedium" />
        <Setter Property="FontSize" Value="14" />
    </Style>

    <Style TargetType="Label" Class="Title.Large">
        <Setter Property="FontFamily" Value="RobotoRegular" />
        <Setter Property="FontSize" Value="22" />
    </Style>

    <Style TargetType="Label" Class="Body.Large">
        <Setter Property="FontFamily" Value="RobotoRegular" />
        <Setter Property="FontSize" Value="16" />
    </Style>

    <Style TargetType="Label" Class="Body.Small">
        <Setter Property="FontFamily" Value="RobotoRegular" />
        <Setter Property="FontSize" Value="12" />
    </Style>

    <Style TargetType="Label" Class="Label.Medium">
        <Setter Property="FontFamily" Value="RobotoMedium" />
        <Setter Property="FontSize" Value="12" />
    </Style>

    <Style TargetType="Label" Class="Label.Small">
        <Setter Property="FontFamily" Value="RobotoMedium" />
        <Setter Property="FontSize" Value="11" />
    </Style>

    <!-- ================================================================ -->
    <!-- ICON BUTTON STYLES (MD3 Standard Icon Button)                    -->
    <!-- ================================================================ -->

    <!-- MD3: Standard icon button — action/trailing role (OnSurfaceVariant) -->
    <Style x:Key="StandardIconButton" TargetType="dx:DXButton">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="IconColor" Value="{StaticResource OnSurfaceVariant}" />
        <Setter Property="WidthRequest" Value="48" />
        <Setter Property="HeightRequest" Value="48" />
        <Setter Property="CornerRadius" Value="24" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <!-- MD3: Standard icon button — navigation/leading role (OnSurface, higher prominence) -->
    <Style x:Key="NavigationIconButton" TargetType="dx:DXButton">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="IconColor" Value="{StaticResource OnSurface}" />
        <Setter Property="WidthRequest" Value="48" />
        <Setter Property="HeightRequest" Value="48" />
        <Setter Property="CornerRadius" Value="24" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <!-- ================================================================ -->
    <!-- UTILITY STYLES                                                    -->
    <!-- ================================================================ -->

    <!-- MD3: FAB (medium, 56×56, CornerRadius=16 = ShapeKeyTokens.CornerLarge) -->
    <Style x:Key="Fab" TargetType="dx:DXButton">
        <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
        <Setter Property="IconColor" Value="{StaticResource OnPrimary}" />
        <Setter Property="PressedBackgroundColor" Value="{StaticResource PrimaryContainer}" />
        <Setter Property="WidthRequest" Value="56" />
        <Setter Property="HeightRequest" Value="56" />
        <Setter Property="CornerRadius" Value="16" />
        <Setter Property="HorizontalOptions" Value="End" />
        <Setter Property="VerticalOptions" Value="End" />
    </Style>

    <!-- MD3: Divider — use Color (BoxView-specific fill), NOT BackgroundColor -->
    <Style x:Key="Divider" TargetType="BoxView">
        <Setter Property="HeightRequest" Value="1" />
        <Setter Property="Color" Value="{StaticResource OutlineVariant}" />
    </Style>

    <!-- Loading skeleton bone — matches ListItem 56dp height -->
    <Style x:Key="SkeletonBone" TargetType="dx:DXBorder">
        <Setter Property="BackgroundColor" Value="{dx:ThemeColor SurfaceContainerHighest}" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="HeightRequest" Value="56" />
        <Setter Property="Margin" Value="0,1" />
    </Style>

    <!-- MD3: Bottom sheet — destructive action button -->
    <Style x:Key="BottomSheetDestructiveAction" TargetType="dx:DXButton">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="TextColor" Value="{StaticResource Error}" />
        <Setter Property="HorizontalOptions" Value="Fill" />
        <Setter Property="HeightRequest" Value="56" />
    </Style>

    <!-- MD3: Bottom sheet — cancel/dismiss action button -->
    <Style x:Key="BottomSheetCancelAction" TargetType="dx:DXButton">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="TextColor" Value="{StaticResource Primary}" />
        <Setter Property="HorizontalOptions" Value="Fill" />
        <Setter Property="HeightRequest" Value="56" />
    </Style>

    <!-- ================================================================ -->
    <!-- EMPTY STATE STYLES                                               -->
    <!-- ================================================================ -->

    <!-- MD3: Empty state — Headline slot -->
    <Style x:Key="EmptyStateHeadline" TargetType="Label">
        <Setter Property="FontFamily" Value="RobotoMedium" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="TextColor" Value="{dx:ThemeColor OnSurfaceVariant}" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
    </Style>

    <!-- MD3: Empty state — Illustration slot (icon-only display button) -->
    <Style x:Key="EmptyStateIllustration" TargetType="dx:DXButton">
        <Setter Property="IconColor" Value="{dx:ThemeColor OnSurfaceVariant}" />
        <Setter Property="IconWidth" Value="64" />
        <Setter Property="IconHeight" Value="64" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="InputTransparent" Value="True" />
        <Setter Property="WidthRequest" Value="80" />
        <Setter Property="HeightRequest" Value="80" />
        <Setter Property="HorizontalOptions" Value="Center" />
    </Style>

    <!-- ================================================================ -->
    <!-- NAVIGATION DRAWER STYLES                                         -->
    <!-- ================================================================ -->

    <!-- MD3: Navigation drawer — Section header label (Label Medium: 12sp Medium) -->
    <Style x:Key="NavDrawerSectionHeader" TargetType="Label">
        <Setter Property="FontFamily" Value="RobotoMedium" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="TextColor" Value="{StaticResource OnSurfaceVariant}" />
        <Setter Property="Padding" Value="16,8,16,4" />
    </Style>

</ResourceDictionary>