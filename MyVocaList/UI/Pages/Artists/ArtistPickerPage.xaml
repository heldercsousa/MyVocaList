<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MyVocaList.UI.Pages.Artists.ArtistPickerPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:appbars="clr-namespace:MyVocaList.UI.Components.AppBars"
    xmlns:lists="clr-namespace:MyVocaList.UI.Components.Lists"
    xmlns:states="clr-namespace:MyVocaList.UI.Components.States"
    xmlns:dto="clr-namespace:MyVocaList.Contracts.DTOs;assembly=MyVocaList.Contracts"
    xmlns:vm="clr-namespace:MyVocaList.UI.ViewModels"
    xmlns:dx="http://schemas.devexpress.com/maui"
    x:DataType="vm:ArtistPickerViewModel"
    BackgroundColor="{StaticResource Surface}"
    SafeAreaEdges="Container">

    <Shell.TitleView>
        <appbars:SearchAppBar
            SearchText="{Binding SearchText, Mode=TwoWay}"
            BackCommand="{Binding BackCommand}"
            Action1Icon="search_outlined"
            Action1Command="{Binding SearchCommand}"
            Placeholder="Search artists..." />
    </Shell.TitleView>

    <Grid>

        <!-- Shimmer skeleton: visible while IsLoading -->
        <dx:ShimmerView IsVisible="{Binding IsLoading}">
            <VerticalStackLayout Spacing="0">
                <BoxView HeightRequest="56" Margin="16,8" CornerRadius="8"
                         Color="{StaticResource SurfaceVariant}" />
                <BoxView HeightRequest="56" Margin="16,8" CornerRadius="8"
                         Color="{StaticResource SurfaceVariant}" />
                <BoxView HeightRequest="56" Margin="16,8" CornerRadius="8"
                         Color="{StaticResource SurfaceVariant}" />
                <BoxView HeightRequest="56" Margin="16,8" CornerRadius="8"
                         Color="{StaticResource SurfaceVariant}" />
                <BoxView HeightRequest="56" Margin="16,8" CornerRadius="8"
                         Color="{StaticResource SurfaceVariant}" />
            </VerticalStackLayout>
        </dx:ShimmerView>

        <!-- EmptyState: visible when searched but no results (list renders on top when HasResults) -->
        <states:EmptyState
            Illustration="search_outlined"
            Headline="{Binding EmptyStateMessage}"
            IsVisible="{Binding IsShowEmptyState}" />

        <!-- Results list: visible while HasResults — renders above EmptyState in z-order -->
        <dx:DXCollectionView
            ItemsSource="{Binding Results}"
            IsVisible="{Binding HasResults}"
            UseRippleEffect="True">
            <dx:DXCollectionView.ItemTemplate>
                <DataTemplate x:DataType="dto:MusicSearchResultDto">
                    <lists:ListItem
                        Headline="{Binding ArtistName}">
                        <lists:ListItem.GestureRecognizers>
                            <TapGestureRecognizer
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SelectResultCommand}"
                                CommandParameter="{Binding .}" />
                        </lists:ListItem.GestureRecognizers>
                    </lists:ListItem>
                </DataTemplate>
            </dx:DXCollectionView.ItemTemplate>
        </dx:DXCollectionView>

    </Grid>
</ContentPage>
