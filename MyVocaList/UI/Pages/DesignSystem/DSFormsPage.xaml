<?xml version="1.0" encoding="utf-8" ?>
<uranium:UraniumContentPage
    x:Class="MyVocaList.UI.Pages.DesignSystem.DSFormsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:uranium="http://schemas.enisn-projects.io/dotnet/maui/uraniumui"
    xmlns:material="clr-namespace:UraniumUI.Material.Controls;assembly=UraniumUI.Material"
    xmlns:local="clr-namespace:MyVocaList.UI.Pages.DesignSystem"
    Title="Forms">

    <uranium:UraniumContentPage.BindingContext>
        <local:DSFormsViewModel />
    </uranium:UraniumContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout Style="{StaticResource PageContainer}">

            <Label Text="ViewModel-Based Form" StyleClass="Headline.Large" />
            <Label Text="Comprehensive form with two-way data binding" StyleClass="Body.Medium"
                   TextColor="{StaticResource OnSurfaceVariant}" />

            <VerticalStackLayout Style="{StaticResource SectionContainer}">
                <Label Text="Personal Information" StyleClass="Title.Medium" />
                <BoxView HeightRequest="1" Color="{StaticResource Outline}" />

                <material:TextField Title="First Name" Text="{Binding FirstName}" />

                <material:TextField Title="Last Name" Text="{Binding LastName}" />

                <material:TextField Title="Email Address" Text="{Binding Email}" />

                <material:TextField Title="Phone Number" Text="{Binding Phone}" />

                <Label Text="Birth Date:" StyleClass="Label.Medium" />
                <DatePicker Date="{Binding BirthDate}"
                            TextColor="{StaticResource OnSurface}"
                            BackgroundColor="{StaticResource SurfaceContainerHighest}"
                            MinimumHeightRequest="48" />
            </VerticalStackLayout>

            <VerticalStackLayout Style="{StaticResource SectionContainer}">
                <Label Text="Address" StyleClass="Title.Medium" />
                <BoxView HeightRequest="1" Color="{StaticResource Outline}" />

                <material:TextField Title="Street Address" Text="{Binding Address}" />

                <material:TextField Title="City" Text="{Binding City}" />

                <material:TextField Title="Country" Text="{Binding Country}" />

                <material:TextField Title="Postal Code" Text="{Binding PostalCode}" />
            </VerticalStackLayout>

            <VerticalStackLayout Style="{StaticResource SectionContainer}">
                <Label Text="Professional Information" StyleClass="Title.Medium" />
                <BoxView HeightRequest="1" Color="{StaticResource Outline}" />

                <material:TextField Title="Company" Text="{Binding Company}" />

                <material:TextField Title="Job Title" Text="{Binding JobTitle}" />

                <material:TextField Title="Website" Text="{Binding Website}" />

                <Label Text="Join Date:" StyleClass="Label.Medium" />
                <DatePicker Date="{Binding JoinDate}"
                            TextColor="{StaticResource OnSurface}"
                            BackgroundColor="{StaticResource SurfaceContainerHighest}"
                            MinimumHeightRequest="48" />
            </VerticalStackLayout>

            <VerticalStackLayout Style="{StaticResource SectionContainer}">
                <Label Text="Additional Details" StyleClass="Title.Medium" />
                <BoxView HeightRequest="1" Color="{StaticResource Outline}" />

                <material:TextField Title="Notes" Text="{Binding Notes}" MaxLength="500" />

                <HorizontalStackLayout>
                    <CheckBox Style="{StaticResource MD3CheckBox}" IsChecked="{Binding IsActive}" />
                    <Label Text="Active Status" StyleClass="Body.Medium" VerticalOptions="Center" />
                </HorizontalStackLayout>

                <HorizontalStackLayout>
                    <CheckBox Style="{StaticResource MD3CheckBox}" IsChecked="{Binding EmailNotifications}" />
                    <Label Text="Email Notifications" StyleClass="Body.Medium" VerticalOptions="Center" />
                </HorizontalStackLayout>

                <HorizontalStackLayout>
                    <CheckBox Style="{StaticResource MD3CheckBox}" IsChecked="{Binding SmsNotifications}" />
                    <Label Text="SMS Notifications" StyleClass="Body.Medium" VerticalOptions="Center" />
                </HorizontalStackLayout>
            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</uranium:UraniumContentPage>
