<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:dx="http://schemas.devexpress.com/maui"
    x:Class="MyVocaList.UI.Components.Sheets.UpdateRequiredBottomSheet"
    x:Name="self">

    <!--
        MD3 Modal bottom sheet — hard update block (non-dismissible).
        AllowDismiss="False" and IsCancelable="False" — user cannot bypass this.
        No grabber shown — reinforces non-dismissibility.
    -->
    <dx:BottomSheet x:Name="Sheet"
                    HalfExpandedRatio="0.35"
                    AllowedState="HalfExpanded"
                    IsModal="True"
                    ShowGrabber="False"
                    AllowDismiss="False"
                    IsCancelable="False"
                    BackgroundColor="{StaticResource Surface}"
                    CornerRadius="28">
        <VerticalStackLayout>
            <Label StyleClass="Title.Medium"
                   Text="Update Required"
                   TextColor="{StaticResource OnSurface}"
                   HorizontalTextAlignment="Center"
                   Margin="24,20,24,4" />
            <Label x:Name="MessageLabel"
                   StyleClass="Body.Medium"
                   TextColor="{StaticResource OnSurfaceVariant}"
                   HorizontalTextAlignment="Center"
                   Margin="24,0,24,16" />
            <BoxView Style="{StaticResource Divider}" />
            <dx:DXButton Content="Update Now"
                         Style="{StaticResource BottomSheetDestructiveAction}"
                         Clicked="OnUpdateNowClicked" />
        </VerticalStackLayout>
    </dx:BottomSheet>

</ContentView>
