<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:dx="http://schemas.devexpress.com/maui"
    x:Class="MyVocaList.UI.Components.Sheets.UpdateAvailableBottomSheet"
    x:Name="self">

    <!--
        MD3 Modal bottom sheet — soft update nudge (dismissible).
        AllowDismiss="True" and IsCancelable="True" so users can tap away.
        Show via code-behind Show(UpdateCheckResult).
    -->
    <dx:BottomSheet x:Name="Sheet"
                    HalfExpandedRatio="0.35"
                    AllowedState="HalfExpanded"
                    IsModal="True"
                    ShowGrabber="True"
                    AllowDismiss="True"
                    IsCancelable="True"
                    BackgroundColor="{StaticResource Surface}"
                    CornerRadius="28">
        <VerticalStackLayout>
            <Label x:Name="TitleLabel"
                   StyleClass="Title.Medium"
                   TextColor="{StaticResource OnSurface}"
                   HorizontalTextAlignment="Center"
                   Margin="24,20,24,4" />
            <Label x:Name="BodyLabel"
                   StyleClass="Body.Medium"
                   TextColor="{StaticResource OnSurfaceVariant}"
                   HorizontalTextAlignment="Center"
                   Margin="24,0,24,16" />
            <BoxView Style="{StaticResource Divider}" />
            <dx:DXButton x:Name="UpdateNowButton"
                         Content="Update Now"
                         Style="{StaticResource BottomSheetDestructiveAction}"
                         Clicked="OnUpdateNowClicked" />
            <BoxView Style="{StaticResource Divider}" />
            <dx:DXButton Content="Later"
                         Style="{StaticResource BottomSheetCancelAction}"
                         Clicked="OnLaterClicked" />
        </VerticalStackLayout>
    </dx:BottomSheet>

</ContentView>
