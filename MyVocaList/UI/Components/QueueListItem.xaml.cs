namespace MyVocaList.UI.Components;

/// <summary>List item component for displaying a queue entry (pending, performing, completed, or absent).</summary>
public partial class QueueListItem : ContentView
{
    public QueueListItem()
    {
        InitializeComponent();
    }
}
