<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:dx="http://schemas.devexpress.com/maui"
    x:Class="MyVocaList.UI.Components.Lists.ListItemLeadingAvatar"
    x:Name="self">

    <dx:DXBorder
        WidthRequest="40"
        HeightRequest="40"
        CornerRadius="20"
        BackgroundColor="{Binding AvatarColor, Source={x:Reference self}}">

        <Label
            Text="{Binding Initials, Source={x:Reference self}}"
            FontFamily="RobotoMedium"
            FontSize="14"
            TextColor="{Binding InitialsColor, Source={x:Reference self}}"
            HorizontalOptions="Center"
            VerticalOptions="Center"
            HorizontalTextAlignment="Center" />

    </dx:DXBorder>
</ContentView>
