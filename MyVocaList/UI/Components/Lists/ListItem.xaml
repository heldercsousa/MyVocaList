<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="MyVocaList.UI.Components.Lists.ListItem"
    x:Name="self">

    <Grid x:Name="container"
          ColumnDefinitions="Auto,*,Auto"
          ColumnSpacing="0"
          MinimumHeightRequest="56"
          Padding="16,0,24,0"
          BackgroundColor="{StaticResource Surface}">

        <!-- Leading slot (Col 0) -->
        <ContentView x:Name="leadingSlot"
                     Grid.Column="0"
                     Margin="0,0,16,0"
                     VerticalOptions="Center"
                     IsVisible="False" />

        <!-- Text column (Col 1) -->
        <VerticalStackLayout x:Name="textColumn"
                             Grid.Column="1"
                             Spacing="2"
                             VerticalOptions="Center">

            <Label x:Name="overlineLabel"
                   StyleClass="Label.Small"
                   TextColor="{StaticResource OnSurfaceVariant}"
                   IsVisible="False"
                   MaxLines="1"
                   LineBreakMode="TailTruncation" />

            <Label x:Name="headlineLabel"
                   StyleClass="Body.Large"
                   TextColor="{StaticResource OnSurface}"
                   MaxLines="1"
                   LineBreakMode="TailTruncation" />

            <Label x:Name="supportingLabel"
                   StyleClass="Body.Medium"
                   TextColor="{StaticResource OnSurfaceVariant}"
                   IsVisible="False"
                   MaxLines="1"
                   LineBreakMode="TailTruncation" />

        </VerticalStackLayout>

        <!-- Trailing slot (Col 2) -->
        <ContentView x:Name="trailingSlot"
                     Grid.Column="2"
                     Margin="8,0,0,0"
                     VerticalOptions="Center"
                     IsVisible="False" />

    </Grid>
</ContentView>
