# MyVocaList - Changelog

## Entries for july 2026
- **07/05/2026** - amend - Rules File Refactoring Task 05 — **`.claude/rules/constraints-registry.md` converted to a routing table** (completes Phase 1 — situational files). **Old:** 74-line all-in-one registry (~3.2k tokens, unconditionally loaded every session and every subagent). **New:** rule reduced to a routing table; full detail moved verbatim to the new cohesive `.claude/library/constraints-reference.md` (`##` sections: DevExpress/UI · .NET MAUI · EF Core/SQLite · Visual Studio Solution · Design/Prototyping · How to add entries). **Inbound `§` anchors preserved (Gotcha 1):** the two `##` headings other files link to by anchor are kept in the routing table — `§ EF Core / SQLite` (referenced by `code-principles.md`) and `§ Visual Studio Solution (.sln)` (referenced by `orchestrator.md` and `workflow.md` ×2). Their never-miss HARD RULE / HARD GATE lines (no C#-side string normalization; `.sln` registration for `Docs/` files) stay inline; the rest points to the library. Also corrected the stale `.sln` GUID counter note (`0040` → `0041`, matching the already-consumed rules-file-refactoring folder GUID). Added a discovery row to the user-level `myvocalist-coding` skill map. One cohesive library file per over-fragmentation guard. Behavioral change: none (container-only). Authorship: applied directly by Claude Code in an overnight autonomous session (pre-authorized by Helder); requires Helder human review per CLAUDE.md § Continuous Enhancement — Authorship.
- **07/05/2026** - amend - Rules File Refactoring Task 03 — **`.claude/rules/bug-tracking.md` converted to a routing table.** **Old:** 86-line all-in-one rule (~1.7k tokens, unconditionally loaded every session and every subagent). **New:** rule reduced to a routing table that keeps the `[HARD RULE]` regression-test-per-severity table + BUG-NNN scheme + severity one-liners inline (never-miss), pointing full detail (BACKLOG nesting example, severity definitions/examples, task-log-entry table, bug-fix workflow) to the new `.claude/library/bug-tracking-reference.md` (moved verbatim, zero content change). **Deviation from tasks.md:** collapsed the planned TWO library files (bug-severity-classification.md + regression-test-requirements.md) into ONE cohesive `bug-tracking-reference.md` per the spike over-fragmentation guard (pilot-findings.md #5) — the same correction already applied to Task 05. No inbound `§` anchors (grep confirmed: sole live pointer is `CLAUDE.md` line 94, filename-only, still resolves). Added a discovery row to the user-level `myvocalist-coding` skill map (out-of-repo). Behavioral change: none (container-only). Authorship: applied directly by Claude Code in an overnight autonomous session (pre-authorized by Helder); requires Helder human review per CLAUDE.md § Continuous Enhancement — Authorship.
- **07/05/2026** - amend - Rules File Refactoring Task 04 — **`.claude/rules/component-change-governance.md` converted to a routing table.** **Old:** 73-line all-in-one governance rule (~1.4k tokens, unconditionally loaded every session and every subagent per GATE-A). **New:** rule reduced to a ~30-line routing table that keeps the `[HARD RULE]` four-gate summary + no-bundling rule + scope trigger inline (they must never be missed) and points all detail to the new `.claude/library/component-safety-gate.md` (full governed-component list, four-gate specifics, required-artifacts template, relationship to `workflow.md` Rules 1–2 — moved verbatim, zero content change). No inbound `§` anchors exist (grep confirmed: sole live pointer is `CLAUDE.md` line 93, filename-only, still resolves). Added a discovery row to the user-level `myvocalist-coding` skill map (out-of-repo, not `.sln`-registered). One cohesive library file (not multiple) per spike over-fragmentation guard. Behavioral change: none (container-only). Authorship: applied directly by Claude Code in an overnight autonomous session (pre-authorized by Helder); requires Helder human review per CLAUDE.md § Continuous Enhancement — Authorship.
- **07/04/2026** - amend - Rules File Refactoring Task 02 — **deleted `.claude/rules/mediatr-patterns.md`** (117 lines, ~1.1k tokens of pure unconditional overhead). **Old:** the file loaded in every session AND every subagent (GATE-A proof) yet documented MediatR command/query/event patterns for code that **does not exist** — MediatR is not registered in `MauiProgram.cs`. **New:** content moved **verbatim** (zero change) to `.claude/library/mediatr-reference.md`, loaded on demand only via the `myvocalist-coding` skill map / a `CLAUDE.md § Rules Files` pointer. Updated the sole live inbound pointer (`CLAUDE.md § Rules Files` line 90) from the deleted rule to the library file, tagged "reference only — not loaded unconditionally"; added a skill-map row (user-level `myvocalist-coding/SKILL.md`, out-of-repo). Historical/archival mentions in `Docs/Changelog`, `Docs/Management/DevCycleCraft/sdd/*`, and `Docs/DevEnv/*` left untouched (they record past state, not live routing). Also this session: reviewed the whole feature's decisions and applied F1–F5 corrections — measured the real baseline (60,492-token subagent cold-start, 0 tools; supersedes the disagreeing 17.2k/28.4k/33.3k estimates), moved the subagent-inheritance check ahead of Phase 1 as GATE-A (PASS), split off GATE-B before the workflow/testing splits, reframed Success Metrics to lead with the sticky per-agent reduction, and narrowed Task 11 skill re-enablement to `brainstorming` + `writing-plans` (dropped TDD/code-review — duplicate the project's customized rules). Behavioral change: none (container + doc-ordering only). Authorship: applied directly by Claude Code (rules write-protection removed 2026-07-03); requires Helder human review per CLAUDE.md § Continuous Enhancement — Authorship.
- **07/04/2026** - amend - Rules File Refactoring Phase 0 spike (PASS) — first `.claude/rules/*.md` converted to routing table. **Old:** `.claude/rules/code-principles.md` was a 268-line all-in-one reference (~3k tokens, unconditionally loaded as a memory file every session). **New:** rewritten to a 44-line routing table; full detail moved **verbatim** (zero content change) to `.claude/library/code-style-reference.md` (263 lines, loaded on demand via the `myvocalist-coding` skill). Net ~2.5k unconditional tokens recovered for this one file. The 6 section headings other files cross-reference by `§` anchor (`Architecture Constraints`, `C# Style / Naming`, `DI Registration Conventions`, `Exception Handling`, `UI Thread Performance`, `Service Return Patterns` — referenced from CLAUDE.md, constraints-registry.md, dialogs-validation.md) are preserved in the routing table so inbound links still resolve. Added a mapping row to the user-level `myvocalist-coding` skill so the new library file is discoverable. Registered `pilot-findings.md` in `MyVocaList.sln`; updated `tasks.md`, `task-log.md`, and BACKLOG (feature 🟢 Ready → 🟡 In Progress, spike ✅ Done). **Executed directly by the main agent (no subagent) per Helder directive** — this feature touches only `.claude/rules|library/*.md` (not `.cs`/`.xaml`), so the orchestrator read-scope rule permits direct edits, and delegating would incur ~55k/subagent cold-start inflated by the very rules bloat being removed. Behavioral change: none (container-only). Findings (`pilot-findings.md`) recorded 4 gotchas that adjust Tasks 01–12: preserve inbound `§` anchors (grep-first), register each new library file in the skill map, defer ALL plugin/superpowers enablement to Task 11, and note `.claude/` subfiles are not `.sln`-registered. ⏳ Helder async-review of spike findings gates Tasks 02–10. Authorship: applied directly by Claude Code; requires Helder human review per CLAUDE.md § Continuous Enhancement — Authorship.
- **07/04/2026** - docs - Rules File Refactoring feature spec, plan, and tasks. Created `Docs/Management/DevCycleCraft/rules-file-refactoring/` with requirements.md (4 ACs: spike validation, plan completeness, context efficiency measured, zero workflow change), design.md (before/after architecture, routing-table format, migration checklist, success criteria), plan.md (4-phase execution timeline over 3–4 weeks, resource allocation, risk mitigation), tasks.md (12 sequential + concurrent tasks: spike + 5 small-file refactors + 5 large-file refactors + 2 skill re-enablement), and task-log.md (spec-writing session). Updated BACKLOG.md: Rules File Refactoring status 🟡 In Progress → 🟢 Ready. Registered all files in MyVocaList.sln under DevCycleCraft/rules-file-refactoring GUID {FA1234BC-0001-4000-8000-000000000041}. Feature targets 70k+ token savings per multi-agent wave by converting 17.2k unconditional `.claude/rules/*.md` load to 2–3k routing tables + 14k on-demand via re-enabled superpowers skills.
- **07/03/2026** - docs - Mapped Helder's manual emulator test session (`EMULATOR_TEST_MASTER_LIST.md`, 12 tests run) into BACKLOG.md and every referenced source file. Phase 16C.1 **FAILED**: hard blocker BUG-027 (Critical) — SongFormPage Artist field has no working required-field validation or autocomplete, blocking all song creation/edit; cascaded to block TEST-002 (BUG-023), TEST-003 (BUG-024), TEST-006 (Song form validation). New bugs registered BUG-027–BUG-039 (SongFormPage artist gap, ArtistsPage catalog-nav regression, ArtistFormPage search crash + UX ambiguity, missing 3rd-party autocomplete spec gaps, shared character-counter duplication on Venue/Artist forms, four PersonFormPage issues). Confirmed passing: BUG-017/BUG-020/BUG-021 (icon + crash fixes), TEST-004 (Venue validation), TEST-005 (Person validation retest), TEST-007 (Artist validation). TEST-012 (Queue BUG-011) blocked — no navigable entry point exists yet, consistent with in-flight Queue Entry Point Redesign. Updated: `BACKLOG.md`, `EMULATOR_TEST_MASTER_LIST.md`, `artists-songs/tasks.md`, BUG-017/019/020/021/023/024 detail files, queue-management BUG-011 file, and the three `form-validation-task-log.md` files.
- **07/03/2026** - amend - Permanently removed `CLAUDE.md` / `.claude/rules/*.md` write-protection. **Old:** `.claude/settings.json` `permissions.deny` blocked `Edit`/`Write` on `CLAUDE.md` and `.claude/rules/*.md` unconditionally; any rule/constant fix in those files (e.g. the `.sln` GUID-counter note in `constraints-registry.md`) required a manual `amend:` handoff to Helder even for trivial, non-editorial fixes, per the "rules dir write-protected" pattern used throughout 2026-06 (session-continuity-leasing, backlog-first-registration, workflow-folder-layout-alignment). **New:** `permissions.deny` is now an empty array — Claude Code may edit these files directly like any other file in the repo. Also fixed the stale `constraints-registry.md` `.sln` GUID counter (said `0014`, actual last-used was `0037` at time of check) to `0040`, and updated `workflow-folder-layout-alignment/findings.md`'s now-obsolete "Step A/C: remove/re-apply write protection" protocol to note the block no longer exists (must not be re-added). Effective 2026-07-03. Rationale: Helder found the block low-value friction for the common case (fixing a stale fact/counter) versus its intended purpose (preventing un-reviewed wholesale LLM rewrites of constitutional docs); explicitly instructed permanent removal. The editorial "human-authored or human-reviewed" policy in `CLAUDE.md § Continuous Enhancement — Authorship` is unchanged — it is a review-quality norm, not a tool-enforced gate, and still applies; only the mechanical permission block was removed. Authorship: this change itself was applied directly by Claude Code per explicit Helder instruction (the tool gate this entry describes removing was the only thing that previously would have prevented that).
- **07/01/2026** - amend - Removed auto-commit-and-push from the `Stop` hook in `.claude/settings.json`. **Old:** Stop hook was an `agent`-type hook that ran `git add -A && git commit -m "chore: auto-commit at session end — build+test green" && git push origin HEAD` unconditionally, and skipped build/test verification entirely (but still claimed "green" in the commit message) whenever no `.cs`/`.xaml` file was among the changes. **New:** Stop hook is now a cheap `command`-type check that only warns ("run dotnet build && dotnet test, then `/project:commit`") — it no longer commits or pushes anything, matching what `workflow.md § Rule 3` already documented the hook as doing ("the Stop hook warns you"). Also updated the `PostToolUse` Agent-matcher hook's reminder text, which previously told agents "the Stop hook will auto-commit at session end." **Root cause:** Claude Code's `Stop` event fires after every response turn, not only true session end (same failure mode already diagnosed and fixed for a different hook — see 06/27/2026 entry below). This produced 254 identical no-information "chore: auto-commit…" commits repo-wide, 41 of which touched `Docs/Management/BACKLOG.md` (82% of all BACKLOG-touching commits), making BACKLOG.md's real change history unreadable in `git log`, and created risk of pushing partial/mid-task work to the checked-out branch without any human or subagent-exit-checklist verification. Discovered while investigating why BUG-020 (fix commit `3b2cb75`, confirmed present and un-reverted on `develop`) still appeared "not fixed" during on-device testing — root cause there was separate (fix verified only via build+unit tests, not on-device repro, per its own BACKLOG note), but the noisy auto-commit history made it hard to trust the trail while investigating. Authorship: requires Helder human review (hooks/settings changes carry the same authorship-review bar as rules files).
