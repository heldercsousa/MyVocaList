---
id: gotosettings-navigation-fix
title: "Bug: GoToSettings navigation exception"
status: "✅ Fixed"
target: 2026-05
section: BusinessFeatures
kind: change
closed: 2026-05
order: 20
goal: "fix navigation crash from SongFormPage to Settings. Fixed with a single-line absolute-route change."
pointer: BusinessFeatures/artists-songs/task-log.md
---

# Bug: GoToSettings navigation exception

Fixed a navigation crash from `SongFormPage` to Settings with a single-line absolute-route change.

> Migrated from the 2026-05 archive row (T12a Wave C). This row has no `BUG-NNN` id (an
> unnumbered fix predating the bug-tracking scheme); because one folder backs exactly one row, it
> is filed as a `changes/` item under `artists-songs/` and `pointer:` stays on the shared
> `task-log.md` — nothing deleted (REQ-SEV-27). Folder shape, slug, title and `order` are
> agent-authored — flagged for the gate audit. Goal text is transcribed verbatim from the archived
> BACKLOG Notes cell (`closed: 2026-05`).
