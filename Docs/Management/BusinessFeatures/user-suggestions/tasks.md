# User Suggestions — Tasks
**Status:** Pending spec approval — tasks to be detailed during plan phase

---

## Placeholder

Tasks will be written after Helder approves `requirements.md` and `design.md`.

High-level phases anticipated:

1. Add `FeedbackSubmission` DTO + `FeedbackCategory` enum to `MyVocaList.Contracts`
2. Add `IFeedbackService` interface to `MyVocaList.Domain`
3. Implement `FeedbackService` in `MyVocaList.Services` + unit tests
4. Add `feedback` route to `Routes.cs`
5. Add `FeedbackViewModel` + `FeedbackPage.xaml/.cs`
6. Add "Send Feedback" entry to `SettingsPage`
7. Register in `MauiProgram.cs`; add GitHub config to `appsettings.json` template
