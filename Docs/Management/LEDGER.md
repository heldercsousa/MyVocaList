# Work Ledger — branch/status tracker (lives on develop)

> **Purpose:** single source of truth, always on `develop`, for WHERE every in-flight task/bug lives (branch, worktree) and its current phase/status. This is what makes resuming development, debugging, and merges into develop possible after any interruption. Maintained via `/sln-ledger` and at every phase transition (dispatch, phase change, merge, close). Rules: `workflow.md § Rule 2` (docs land on develop).
>
> **Row lifecycle:** add the row when work is dispatched to a branch/worktree; update `Phase/Step`, `Status`, and `Last commit` at each transition; on merge into develop set Status `merged` and move the row to the Completed section (keep last 20 for audit, prune older).

## Active

| ID | Feature / Bug | Phase / Step | Branch | Worktree | Status | Last commit | Next action |
|----|---------------|--------------|--------|----------|--------|-------------|-------------|
| DX-AC | Replace AutocompleteMobileField with DX AutoCompleteEdit | T2–T6 complete; merged to develop (verifier CONDITIONAL PASS, W1/W3 fixed, W2 = T7 watch item); 501/501 green; worktree removed; pushed | merged into develop | — | merged | e17cb4c | **Updated 2026-08-03 — items (2) and (3) are DONE, not pending.** T7 was run; it surfaced BUG-050, BUG-051 and BUG-052, all three since fixed and ✅ closed. The REQ-DXAC-03 blur conflict resolved in favour of retaining typed text (re-run #5 item D3 ✅), so **BUG-027 IS closed by this swap** — closed ✅ Fixed 2026-08. BACKLOG rows closed 2026-08-03. Remaining: confirm no residual DX-migration defect, then set this row ✅ and move it to Completed. |
| INLINE-AC | Song artist field — correctness fixes (BUG-050/051/052 + retain-text) + inline create-new-artist | **MERGED to develop 2026-08-18.** The branch was discovered NEVER merged to any branch — Helder hit an already-fixed bug (typed artist text cleared on blur, REQ-ACREATE-03) while testing the UOW pilot, and the 25 commits were found orphaned on `feat/inline-artist-create`. Merged to develop as `71926980` (one code conflict, `Services/SongService.cs`; verifier PASS — BUG-067 artist-FK fix preserved verbatim, redundant service-layer `.Trim()` calls correctly dropped since EF `TrimValueConverters` handle it per design.md § D3). Then carried onto `feat/uow-pilot`. **T10 re-run: superseded** — Helder's 2026-08-18 emulator gate on the merged UOW branch exercised inline artist create, edit-artist, edit-title and delete for both Songs and Artists, all green. BUG-069/070 were never separately closed and remain open if still reproducible. | `feat/inline-artist-create` (merged, deletable) | — (worktree removed) | merged | 71926980 | Helder: confirm whether BUG-069 (dropdown re-opens after selection) and BUG-070 (misleading 'search and select' copy) still reproduce on the merged build; close or re-register them. |
| ITF | Inline Trivial Fix lane — bounded orchestrator inline-edit exception | Rules amended + Guard 3 merged (verifier CONDITIONAL PASS, all findings resolved); worktree-base-check false-positive fix merged; 38/38 green on develop; worktrees removed | merged into develop | — | merged | a4c198a | Helder: observe the FIRST LIVE ITF FIX end-to-end (last open gate) — opportunistic, waits for a qualifying fix. Full resume instructions incl. how to recognise/run one: `DevCycleCraft/inline-trivial-fix/handoff.md`. Also awaiting a decision on the 4 misbased `agent-*` worktrees (1 unmerged commit each — listed in that handoff). |
| UOW | DbContext lifetime & unit-of-work pattern (MAUI has no per-page scope) | **Phases 0–3 COMPLETE and MERGED to develop 2026-08-18 (`cdec7af5`), 590/590 green, pushed.** Phase 3.3 emulator gate PASSED (Helder) — Songs and Artists create/reopen/edit/save/delete with no tracking-conflict exception and every edit persisting. All 9 pilot methods wrapped across SongService, ArtistService, ArtistResolutionService, SongResolutionService; every Phase 0 RED went GREEN. **REQ-UOW-18 was NOT vacuous after all** — the earlier `git merge-base` verdict was an artifact of `feat/inline-artist-create` never having been merged; the `1a114c1` stopgap arrived with that merge and is now deleted per the requirement. **Deleting it exposed BUG-067 re-opening through the UOW path:** `SongRepository.GetByIdAsync` eager-loads `OriginalArtist`, so in a fresh context `DbSet.Update` attaches the graph and EF FK fixup reverts `ArtistId`. Fixed by nulling the navigation before assignment in `SongService.UpdateSongAsync` (Helder ruled service-level placement over repository-level). Also fixed a positional-arg hazard: develop's new `int? artistId` parameter sits before `ct`, so both `SongResolutionService` call sites now pass `ct:` by name. `SongServiceUpdateIntegrationTests` migrated to `UnitOfWorkTestHost` (retired 5-arg ctor; Helder accepted — no assertion softened). **Still load-bearing:** (a) BUG-071 is NOT gone app-wide — Queue/Event keep the defect until Phase 3.5; (b) do not touch files carrying `TODO [BUG-071 / UOW]`; (c) REQ-UOW-28 (resolve from the lambda's own `sp`, never `_`-fields) governs every future wrap. BUG-075 (pre-commit hook needs `--no-restore`) and BUG-076 (flaky SQLite `ObjectDisposedException`, seen once this session) remain open. | merged into develop | — (worktree removable) | merged | cdec7af5 | **Event/Queue DELETED 2026-08-20 — merged to develop (`32e7a85e`, branch `chore/demolish-event-queue`, worktree removed).** 49 files, +72/−3104. All Event/Queue code, tests, DI registrations and hamburger access removed; **only infra definitions kept** (both `Event` entities, `Venue.Events`, all four EF configs, `AppDbContext` DbSets, every migration) — **no migration created, schema unchanged**. **Venues is now the app's landing page** (QueuePage had been Shell's initial content); its exit-confirmation sheet was ported, and a review-caught regression fixed (the port shadowed `CrudListPageBase.OnBackButtonPressed`, breaking delete-confirm dismissal). Docs reduced to the spec (`queue-management/README`+`requirements`+`design`) for a future re-plan. Tests 592→570, green on develop. **Phases 0–3 (the Songs/Artists UOW pilot) remain valid and shipped.** Nothing further to do here — next Queue work starts from a new spec. |
| COPY-YR | About page copyright year — auto-extending founding-to-current range | **MERGED to develop 2026-08-24 (`2b6fc488`, --no-ff).** The implementor had in fact done the work but left it UNCOMMITTED in the worktree and never returned — it was found as a dirty working tree at the branch's base commit. Cause of the stall: the implementor's second test constructed `AboutViewModel`, whose ctor reads `AppInfo.VersionString`, a MAUI platform API that throws `NotImplementedInReferenceAssemblyException` off-device. Rewritten to assert the constant-to-formatter wiring statically instead; no assertion softened, the formatter cases (range / same-year collapse / skewed-clock) are untouched. Implementation is a pure static `FormatCopyright(foundedYear, currentYear)` bound via `AboutViewModel.Copyright`; `Since 2025` deliberately unchanged. 597/597 green in the worktree, 575/575 on develop after merge (the 22-test delta is the Event/Queue deletion, which the branch predated). | `feat/about-copyright-year` (merged, deletable) | `../MyVocaList-copyright-year` (removable) | ✅ closed | 2b6fc488 | **CLOSED 2026-08-25 — Helder confirmed on-device: the line renders correctly.** Status set `✅ Done` / `closed: 2026-08`; merged branch `feat/about-copyright-year` deleted (verified merged into develop first); its worktree was already gone. Historic note kept: This code was never in a build he ran — his 2026-08-20 About-page sign-off covered the MIT license text and BUG-077 navigation only. Confirm the line renders `© 2025–2026 Helder Sousa`, then set the row ✅. |
| MIT-LIC | Relicense to MIT + About page license text + BUG-077 (flyout About never navigates) | **Code MERGED to develop 2026-08-20 (`12102115`, --no-ff, both parents verified).** Root relicensing on develop (`d3c52f7a`). T1: AboutPage.xaml license literals -> "MIT License" / "Free to use, modify, and distribute." (AC-AB-05a/05b). BUG-077 root cause: `NavigationConfig.PageTypes` had no `Routes.About` key, and `AppShellViewModel.NavigateAsync` `return`s silently on a `TryGetValue` miss - no exception, no log, so the About page was unreachable from its only entry point since it shipped. Fixed with one dictionary entry plus `NavigationConfigTests` (new, Red->Green) which asserts EVERY non-special-cased menu route resolves, closing the failure class. Orchestrator re-ran verification independently: build 0 errors, 592/592 green. | `fix/about-page-license-and-nav` (merged, deletable) | - (worktree removed) | merged | 12102115 | **CLOSED 2026-08-20 — Helder confirmed the manual E2E PASSED on device.** - Flyout -> About opens the page and the License section renders the MIT text. Both BACKLOG rows are now ✅ (BUG-077 Fixed, license change Done), closed 2026-08. Unregistered follow-ups noted in the BUG-077 README: (1) `NavigateAsync` should log a warning on an unknown route so the next occurrence self-diagnoses at runtime; (2) the About feature was closed ✅ Done with an unexercised manual-E2E gate - worth reviewing how UI-only gates get signed off; (3) `hamburger-nav-pattern` has a `.sln` solution-folder GUID but no `Docs/` spec folder. |
| UOW-4.1 | UoW Phase 4.1 — wrap `CatalogService` in `IUnitOfWork` | **MERGED to develop 2026-08-24 (`e91947f6`, --no-ff).** Both write methods now run through `_uow.ExecuteAsync`, resolving `ICatalogRepository` from the lambda's own `sp` (REQ-UOW-28); `RemoveSongFromCatalogAsync`'s `ExecuteDeleteAsync` path is brought under the explicit transaction (REQ-UOW-33); `SaveChangesAsync` retired from `ICatalogRepository` + `CatalogRepository`. `GetPagedCatalogForArtistAsync` deliberately left unwrapped — consistent with how `SongService`/`ArtistService` treat their own paginated reads. The implementor found **9** direct `_repo.SaveChangesAsync()` call sites in `CatalogRepositoryTests`, not the 2 the stale plan named; all rewritten to `_db.SaveChangesAsync()`, a mechanical substitution with no assertion weakened. Orchestrator re-verified independently: 0 errors, 575/575 on the branch and again on develop after merge. DI needed no edit — `CatalogService` is container-resolved (`ServiceCollectionExtensions.cs:52`), so the new ctor parameter wires itself. | `feat/uow-phase-4-1-catalog` (merged, deletable) | — (worktree removed) | merged | e91947f6 | None. Next UoW work is 4.2–4.7, each with stale coordinates to re-verify. |
| CATALOG-PROP | Song writes propagate to the artist catalog | Status 📋 Spec. All four design decisions closed by Helder 2026-08-24 and recorded in the change folder's `README.md`: D1 catalog = the performer relation, authorship implies a mandatory derived row; D2 reassignment moves the derived row and derived rows are read-only to the user, guarded in Services; D3 UoW 4.1 first (**done**, merged above); D4 existing data backfilled once by an EF migration. Two trace findings the spec must own: `SongService` writes nothing to `Catalog` today, and `SongFormViewModel` has NO latent catalog wiring — the original "SongFormPage already anticipates this" belief is unsubstantiated, the only hit being a cosmetic count label in the artist autocomplete. Also noted: `SongsViewModel.AddToCatalogAsync` is an empty `// TODO Phase 15` stub. | — (spec only, no branch yet) | — | in progress | e91947f6 | **Audit 2026-08-25 — KEEP, do not delete.** Checked against the in-flight `READ-SCOPE` change on the suspicion the two overlapped: **they do not conflict.** READ-SCOPE is infrastructure (routing existing reads through `IUnitOfWork`, no behaviour change); this is unbuilt business behaviour (the derived-catalog-row invariant), and D1–D4 are the only record of Helder's domain decisions. Two things did change: **D3's premise sentence is now stale** — it says `CatalogService` has no `IUnitOfWork`, but Phase 4.1 landed and `CatalogService.cs:17` is now `(ICatalogRepository, IUnitOfWork, ILogger)`, so D3's gate is **satisfied**; and a **new sequencing constraint** — READ-SCOPE's Waves 4.2/4.3 own `SongService.cs` and `CatalogService.cs`, the two files this item must modify, so it is **not dispatchable until READ-SCOPE's Wave 4 is committed**, and the propagation code must be written in the `sp`-resolved lambda shape or Wave 7's architecture test will reject it. Recorded in the item's `README.md § Status check`. Also still true: the spec-writer was NEVER completed — the folder still holds `README.md` only.** The session diverted to the UoW read-scoping work (row `READ-SCOPE`) and this item did not progress. D1–D4 remain closed and valid; the next session must actually write `requirements.md`/`design.md` from them, then spec-reviewer, then Helder's approval gate, then plan. |
| READ-SCOPE | Scope all service reads through `IUnitOfWork` (REQ-UOW-36…52) — fixes BUG-078, unblocks UoW Phase 4.7 | **Spec APPROVED by Helder 2026-08-25 with one amendment; `tasks.md` + `plan.md` written and reviewed. Now BLOCKED ON HELDER'S PLAN-APPROVAL GATE.** Still docs-only — no code, no branch, no worktree. Amendment (**D8**): minimum query length decided from researched community practice — **2 chars local DB, 3 chars remote HTTP**, as two named constants (REQ-UOW-51/52; `design.md § 2c`). 2 is already the majority existing behaviour, so exactly two call sites change; **debounce deliberately excluded** as UI timing owned by the future autocomplete feature. Plan: twelve tasks, nine waves (0–8), ten dispatch rounds. `plan-reviewer` verdict **APPROVE WITH CHANGES** — 0 Critical, 5 Major, 7 Minor, **all 12 applied**; the Majors were an unscheduled census-wide walk (now task 7.2, the artifact Wave 8's limb-(a) precondition depends on), Waves 4/5 owning no test files while their ACs mandate tests, threshold tests with nowhere to live without breaching the REQ-UOW-49 carve-out, REQ-UOW-42's gate-removed run being unschedulable in Wave 6, and a "three comments" miscount in Wave 3 that would have breached the carve-out. Origin: the Phase 4.6b audit conclusion was wrong — "every service is in the pattern" was reasoned per-*service* when it had to be per-*method*. Writes were migrated in 4.1–4.5; **reads were never in scope**. Helder chose the broad fix ("scope all reads to close 4.7") over the narrow one, because `DbLoadGate` cannot be removed while reads share the captive app-lifetime `AppDbContext`. Scope: 25 repository dereferences across 9 services, re-derived twice by Python walk (never `grep` — it returned false zeroes twice this session). Rationale is DbContext **lifetime**, NOT tracking (already globally `NoTracking`) and NOT transactions (`ExecuteReadAsync` opens none) — `design.md § 2` records that correction explicitly. HTTP and file/zip I/O stay outside the scope. The two suggestion services gain `IUnitOfWork` but **no DI registration** — Helder confirmed they are pre-built for the `ArtistFormPage`/`SongFormPage` autocomplete feature, so `MauiProgram.cs` appears in no wave. **Wave order deliberately inverts DRY Onion** (decision D6): the BUG-078 Red must be written while `.AsTracking()` is still present, or the staleness cannot occur and the mandatory fail-before evidence is unobtainable. Two spec-reviewer passes: REJECT (3 Critical) → APPROVE WITH CHANGES; all 24 findings applied. Pass #2 verified ~40 line references with **zero** mismatches, against five wrong in pass #1. | — (spec only, no branch yet) | — | blocked: awaiting Helder approval | f630b8f8 | **Helder: approve or amend the spec.** Look at (1) the inverted wave order in `design.md § 7`, (2) REQ-UOW-47's `DbLoadGate` two-limb removal gate, (3) REQ-UOW-50's architecture test — the only guard that outlives the landing commit. On approval: write `tasks.md` + `plan.md`, dispatch plan-reviewer, then implement in a worktree based on `develop`. Resume detail + context manifest: the item's `task-log.md § Checkpoint`. |
| ORPHAN-APPSET | `app-settings` plan stranded on an agent branch | **RESOLVED 2026-08-25 — DO NOT MERGE.** Compared both files directly (Python, not `diff`): the branch's 347-line `plan.md` and develop's 65-line one cover the **same two gap fixes** (route flyout Preferences → SettingsPage; refresh `HasYouTubeApiKey` in `SongFormPage.OnAppearing`). The branch version is not extra scope — it is the same plan written as verbose step-by-step agent instructions with code snippets. **Both phases shipped 2026-05-31, reviewed PASS** (`app-settings/task-log.md`), item `✅ Done` closed 2026-06; `MyVocaList/UI/Pages/Preferences/` is absent from the tree, confirming Phase 1 landed. Merging a pre-implementation how-to for work completed three months ago would add noise and contradict the item's Done status. Nothing unique was found in it — its code snippets match what the task-log records as actually built. **Byproduct fix:** develop's `app-settings/tasks.md` and `plan.md` still had UNCHECKED boxes for the shipped work (a Rule 4 violation — tasks.md is source of truth); both corrected with a closing note. | — (deleted 2026-08-25, local **and** origin) | — | ✅ closed | 47a021ad | **Helder authorised deletion 2026-08-25.** The branch existed on origin as well as locally, so both copies were removed (`git branch -D` + `git push origin --delete`); verified absent from loose refs, packed refs and `ls-remote`. The commit `47a021ad` (2026-05-30, `docs: add implementation plan for App Settings`, 347 lines + 1 `.sln` line) remains reachable in the local object store until `git gc` prunes it — that is the only remaining copy, and it is not a durable one. Abandoning it was correct: the plan described work that shipped the very next day, 2026-05-31. |
| ORPHAN-NAVSPIKE | Hamburger-nav slide-animation spike stranded on a branch | **CLOSED 2026-08-25 — branch destroyed on Helder's instruction ("no needed anymore").** Branch `worktree-spike+hamburger-nav-animation` (`9b07d8da`, 2026-07-11, 9+/6- in `AppShellViewModel.cs` only) deleted; verified gone from both loose and packed refs, and it had **no remote copy**, so the spike code is unrecoverable by design — correct for throwaway spike code, which must never merge. No `findings.md` was written and none will be: Helder judged the spike no longer relevant. | — (deleted) | — | ✅ closed | 9b07d8da | Conclusion: abandoned as no longer needed. No docs artifact. |
| SC-ENH | Session Continuity enhancements (Phase 8, Tasks 11–15) | complete — verifier PASS; cleanup executed (30→8 worktrees, form-ux merged `f00543a`, 478/478) | develop (tooling scripts + docs only, no app code) | — | to review | 68e1852 | Helder: run two-terminal live demo (last open gate); optionally direct the 4 leftover agent worktrees |

## Completed (last 20)

| ID | Feature / Bug | Branch | Merged into develop | Commit |
|----|---------------|--------|---------------------|--------|
| TRIM-PERSIST | String trimming on persistence — Task 6 EF Core `ValueConverter`s (Task 7 integration merge) | `feat/persisted-string-trimming-converters` | 2026-08-04 — merge `761a1f0f`; conflict-free. develop merged into the branch first (`d08636db`) so develop never held an unverified state. On develop after merge: build 0 errors, 519/519 green, 6/6 PersistedStringTrimmingTests (real SQLite). Task 6 had been complete but UNMERGED since 2026-07-19 — `TrimForStorage` had zero production call sites on develop until now. **Row stays 🟡 In Progress:** all tasks checked, but Helder's on-device E2E gate (REQ-TRIM-01/02) is unperformed. **Worktree `../MyVocaList-wt-trim-persist` still present — safe to remove.** | 01625df |
| SEARCHBAR | AppBar / SearchAppBar Interaction Redesign — persistent MD3 SearchBar in CrudListView (T1–T11) | `feature/persistent-searchbar` | 2026-08-03 — merge `9b462c4`; build 0 errors, 513/513 green. 4 conflicts resolved additively (changelog, .sln, task-log, generated BACKLOG). BACKLOG row ✅ Done. **Worktree `../MyVocaList-wt-searchbar` still present — safe to remove.** Carried forward, NOT part of this closure: BUG-048 (CrudListView pagination reload) + BUG-049 (VenueFormPage post-save nav), both pre-existing, neither registered via `backlog_gen.py`. | 997e784 |
| SPEC-EVO | Spec Evolution — nested `bugs/`/`changes/` folders + generated BACKLOG | merged into develop | 2026-08-02 — ✅ Done, closed 2026-08. Row moved here from Active per the lifecycle rule (merged ⇒ Completed). No open actions. FUP-1 (folder-collision inversion) remains deferred by Helder; the BUG-022 parent/path regen warning is cosmetic metadata on a closed bug. | 9f14807 |

---

**Status vocabulary:** `dispatched` · `in progress` · `build failure` · `test failure` · `to review` · `blocked: <reason>` · `ready to merge` · `merged` · `abandoned: <reason>`

**Column notes:**
- **Branch** — the branch holding the changes (empty = work happened directly on develop, docs-only).
- **Worktree** — `.worktrees/<name>` path, or `—` if the worktree was already removed (branch still holds the commits).
- **Last commit** — short hash on the task branch, so a resuming session can `git log <hash>..<branch>` instantly.
- **Next action** — one line: what the next session should do (e.g. "fix failing test X", "await Helder review", "merge after wave 2").

**Resume chain (no globbing):** this ledger row → the feature's `task-log.md` `### Checkpoint` block (live step state, pinged on a ~10-min heartbeat) → read ONLY its Context manifest files. See `session-ops.md § Checkpoint Ping & Context Manifest`.
