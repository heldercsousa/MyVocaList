# Work Ledger — branch/status tracker (lives on develop)

> **Purpose:** single source of truth, always on `develop`, for WHERE every in-flight task/bug lives (branch, worktree) and its current phase/status. This is what makes resuming development, debugging, and merges into develop possible after any interruption. Maintained via `/sln-ledger` and at every phase transition (dispatch, phase change, merge, close). Rules: `workflow.md § Rule 2` (docs land on develop).
>
> **Row lifecycle:** add the row when work is dispatched to a branch/worktree; update `Phase/Step`, `Status`, and `Last commit` at each transition; on merge into develop set Status `merged` and move the row to the Completed section (keep last 20 for audit, prune older).

## Active

| ID | Feature / Bug | Phase / Step | Branch | Worktree | Status | Last commit | Next action |
|----|---------------|--------------|--------|----------|--------|-------------|-------------|
| DX-AC | Replace AutocompleteMobileField with DX AutoCompleteEdit | T2–T6 complete; merged to develop (verifier CONDITIONAL PASS, W1/W3 fixed, W2 = T7 watch item); 501/501 green; worktree removed; pushed | merged into develop | — | merged | e17cb4c | **Helder: (1) ✅ pushed — origin/develop = local at `e17cb4c`; (2) DECIDE the REQ-DXAC-03 conflict (VM clears artist text on blur — see task-log ⚠ ESCALATION; may mean BUG-027 is not closed by this swap); (3) run T7 on-device checklist** in task-log.md (items a–i incl. smoke 16C.1 + BUG-044/045/047 + BUG-027 re-verify), then close BACKLOG rows |
| INLINE-AC | Song artist field — correctness fixes (BUG-050/051/052 + retain-text) + inline create-new-artist | T1–T9 done + BUG-053 fixed (`8d33547`); **T10 on-device FAILED 2026-07-22** — Part A (BUG-053) ✅; 6 defects remain → **BUG-054…059** (root-caused to DX AutoCompleteEdit wiring/XAML, file:line in task-log § T10 outcome). Fix wave done (code review CONDITIONAL PASS, 520/520). **T10 re-run #2 (2026-07-23): a/e/j/C1/C2 ✅; b/c/i FAIL + new defects.** New/reopened: BUG-060 (artist locks permanently), BUG-057 (reopened — error text invisible), BUG-059 (reopened — catalog empty, not a cascade), BUG-061 (lingering suggestion row); separate/governed: BUG-062/063 (Songs list). **Fix batch 2 (Sonnet, `b0e45da`, 523/523):** BUG-060 (ClearArtistCommand+ClearIconClicked), BUG-057 (ArtistErrorText never set — 1-line), BUG-061 (SelectedItem=null reset) ALL FIXED. **BUG-059 CANCELLED (Helder 2026-07-23 — works-as-designed):** Catalog join table is deliberately picker-only; empty catalog after Song-form save is by design. Reframed as a NEW enhancement (auto-link artist-OWNED songs to author's catalog, perpetual/unremovable hook; manual catalog = performers/covers only) — seed: `BusinessFeatures/artists-songs/ENHANCEMENT-artist-owned-song-catalog-autolink.md`; register in BACKLOG when SPEC-EVO migration settles. T10 item i to be dropped. So INLINE-AC's own defects are ALL fixed (060/057/061 + earlier wave); only Helder's on-device T10 re-run #3 (b/c/change-artist/lingering-row) gates closeout. ⚠️ Android build blocked locally by `XARLP7024` (AV/EDR file corruption on AndroidX AAR extract, NOT code — verified 0 errors on net10.0 TFM); Helder builds Android locally. **T10 re-run #3 (2026-07-25): item 1 (BUG-060 change-artist/unlock) ✅ PASSED-closed;** items 2&3 re-fixed this session — BUG-064 (`283866f`, duplicate error label removed) + BUG-061 completion (`7c594e2`→`3e38066`→`b8f7d2c`, `_suppressNextArtistSearch` guard on ALL 7 programmatic `ArtistSearchText` sites; +7 tests). Verifier CONDITIONAL PASS, no blockers, all residuals closed. **HEAD `b8f7d2c` PUSHED, 530/530 green.** All code work done — only Helder's on-device **T10 re-run #4** (re-verify items 2&3 only) gates closeout (see handoff.md § HELDER'S DUTIES). | `feat/inline-artist-create` | `../MyVocaList-inline-ac` | blocked: T10 re-run #4 (Helder, on-device) | b8f7d2c | **Next session (fix wave in the SAME worktree, sequential):** order BUG-056 (search race) → BUG-055 (edit hydration + edit-save ArtistId) → XAML cluster BUG-054b/057/058 → BUG-054a code-behind → BUG-059 (cascade verify). Incremental single-file XAML edits. Then re-run T10 on-device (Helder). On all-green: apply the closeout in `changes/2026-07-21-inline-artist-create/pending-backlog-closeout.md` — ⚠️ do NOT hand-edit fenced `BACKLOG:GENERATED` rows (SPEC-EVO owns BACKLOG since 2026-07-22; use `backlog_gen.py status`), then merge+push (creds cached), remove worktree, unblock catalog. Full triage: task-log § T10 outcome. |
| ITF | Inline Trivial Fix lane — bounded orchestrator inline-edit exception | Rules amended + Guard 3 merged (verifier CONDITIONAL PASS, all findings resolved); worktree-base-check false-positive fix merged; 38/38 green on develop; worktrees removed | merged into develop | — | merged | a4c198a | Helder: observe the FIRST LIVE ITF FIX end-to-end (last open gate) — opportunistic, waits for a qualifying fix. Full resume instructions incl. how to recognise/run one: `DevCycleCraft/inline-trivial-fix/handoff.md`. Also awaiting a decision on the 4 misbased `agent-*` worktrees (1 unmerged commit each — listed in that handoff). |
| SPEC-EVO | Spec Evolution — nested `bugs/`/`changes/` folders + generated BACKLOG | **⚠️ OWNS `BACKLOG.md` + the 5 `backlog-archive/*.md` generated regions since 2026-07-22.** Phase 1 generator merged. F2/F3 generator fixes merged to develop (`ed20260`) then merged INTO the migration worktree (develop now an ancestor); 129 tests green on the branch. Phase 3 folders **complete through T11c** (T11a `e9396f6`, T11b, T11c + F6 `cf8bd35`). **T12 gate REDEFINED (Helder 2026-07-23).** T12a inventory = 105 archived rows; byte-match impossible (renderer canonicalizes: `Goal:` prefix, drops `↳`, appends `(under:)`). Decisions: F-2 canonical rewrite + idempotency gate (G1 regen-twice, G2 grep-reachability, G3 named archive-migration diff class — REQ-SEV-25's 4 live-BACKLOG classes NOT widened); F-3 extend STATUSES (Superseded/Duplicate); D keep BUG-022 Major. Spec amended + spec-reviewed (1 blocking 🔵-overload gap fixed: TERMINAL keys on full string, not emoji; +4 fixes). **T12-pre DONE** (`e7b29a5`, extend STATUSES Superseded/Duplicate, 143 tests green). **T12a Wave A DONE** (`b862248`, GUIDs 007B/007C). **Wave B unblocked 2026-07-23:** Helder ruled "allow + warn (align to design §2)" on the BUG-022 parent/path-parent contradiction — REQ-SEV-21 amended (`480ba2a`, error→warning). Next: implementor downgrades `model.validate()` parent check to a warning (+test), then registers the 4 already-authored+staged Wave B READMEs in `.sln` from GUID **007D**, validates (expect 0 err / 1 warn), commits. Then Waves C–G → T12 (canonical rewrite + G1/G2/G3 gate) at Architectural review. F-1/F-4/F-5 + all authored goals/slugs/order flagged for Helder's gate audit. | `feature/backlog-migration` | `../mvl-backlog-migration` | **✅ T12a COMPLETE (all 19 waves A–U, `a4514ef`) — every archived row 2026-03→07 is folder-backed. PAUSED at T12 Architectural gate (Helder).** All waves pushed (origin==local @ `a4514ef`), 144 tests green, `.sln` high-water GUID `00D0`, BOM+CRLF preserved. Rulings applied: REQ-SEV-32 (3 (iv) rule-file/changelog rows correctly got NO folder — Blocker #1 CLOSED, not outstanding); BUG-008→06 fence (#3); crud-form-action-pattern Venues split (#2); session-continuity re-triage done in Wave C (#4). **✅ T12 PASS (2026-07-25, `75a6dc9` canonical rewrite + `64ce6da` addendum + `49faf69` task-log; pushed origin==local @ `49faf69`).** G1 zero-diff idempotent · G2 155/156 reachable (1 = `2026-06-mvp-release` milestone, no pointer by design) · G3 all diff lines in named classes · 144 tests · `regen --check` exit 0, 0 staleness. Canonical archive now generated + lossless. REQ-SEV-32 amended (external-pointer rows DO get minimal `cross-cutting/` folders); 3 rows restored (vs-solution-file-registration-rule / proactive-backlog-entry-rule / visual-theme-refresh), GUIDs 00D1–00D3. **✅ T12b DONE (`6f08970`, pushed)** — R-2 pre-commit gate installed in `.claude/githooks/pre-commit` (extends the existing dotnet-test hook; staging any README/BACKLOG/archive file runs `regen --check`, blocks stale commits, `--no-verify` bypass; demo-verified). 144 tests. **ALL AUTONOMOUS WORK COMPLETE — 2 items remain, both need Helder:** (1) **T13a–d rules bundle** — amends `.claude/rules/*` → HARD authorship gate (CLAUDE.md § Authorship): Helder must read before commit. Includes FUP-3 (fix stale `0041`→`00D3` GUID counter in constraints-registry.md) + T13d (retire the exception-registry "docs-on-branch" row). (2) **Merge-back** — the whole T12a/T12/T12b history is unmerged on the branch; merging to develop must resolve the ONE `.sln` union (develop=inline-ac regs vs branch=backlog regs; no other file overlaps — verified). Helder decides merge timing/method. Branch is clean, regen-idempotent, all pushed @ `6f08970`. HISTORY: T12 first run correctly STOPPED on H1 data-loss (folderless rows dropped by generated-archive regen); Helder ruled minimal folders; re-run passed. First run found H1 data-loss: the 3 external-pointer rows (constraints-registry / workflow.md / changelog pointers) have NO folder → real `regen` (which rewrites archives from frontmatter only) silently DROPS them. REQ-SEV-32 "no folder" conflicted with REQ-SEV-27 "nothing deleted". **Helder ruled 2026-07-25: give the 3 rows minimal `cross-cutting/<slug>/` folders** (data-preservation wins). Addendum wave (amend REQ-SEV-32, create 3 folders, GUIDs 00D1+, register POST-MIGRATION-FOLLOWUPS.md) THEN re-run canonical `regen` + G1/G2/G3. Prior run confirmed the regen diff is otherwise CLEAN/in-class (only 2026-07 archive changed, 10 lines); after the 3 rows are restored the diff also inserts them (archive-migration class). STOP-and-escalate on any gate failure. **RESEQUENCED:** `develop` merge-in is NOT a T12 prerequisite — verified the only develop/branch overlap is `MyVocaList.sln` (develop=inline-ac, branch=backlog regs); develop did NOT touch BACKLOG.md/archives, so T12 regen is self-contained on the branch. The `.sln` union + develop merge deferred to **merge-back time** (do it once, not twice). **Folder-collision issue DEFERRED by Helder 2026-07-25** → recorded in `spec-evolution-versioning/POST-MIGRATION-FOLLOWUPS.md` (FUP-1), tackle at those items' closeout. **Consolidated audit set for Helder's ONE gate pass** (all logged in task-log per-wave "To Review" entries): ~38–41 agent-authored slugs/titles + all `order` values; reworded banned-content goals (Waves D/F/K/M/O/R — file paths, N/M fractions, k-token counts, commit hashes); dropped pre-scheme severities (BUG-003 Medium, BUG-004 High, BUG-008 Medium — preserved in flat-file bodies); folder-collision disambiguations (queue-management, backup-restore, ui-form-validation-guide → changes/ sub-items; form-validation-update-{songs,artists,venues,persons} id splits); crud-form-action-pattern Venues split. Resume chain: this row → task-log Checkpoint → T12a-remaining-waves-plan.md. Wave B `26db20c`/`ae40751`/`8229b0c`. Plan `T12a-remaining-waves-plan.md`: **87 rows → 19 waves (C–U)**, ~38–41 authored slugs (flag the set for Helder's gate audit at T12). **Rulings:** (1) non-Docs-pointer rows (`.claude/rules/*`, `changelog.md`) → NO folder, keep archive text (add REQ line to requirements.md); (2) CRUD Form Action Pattern → Venues precedent (2 distinct items, distinct id+order); (3) BUG-008 → route by `closed:06` → move to `BACKLOG-ARCHIVE-2026-06.md`; (4) session-continuity re-triage → fold into Wave C. BUG-015/016 gap (2 extra folders, no decision). **Plan (c): execute Wave C ONLY, validate, commit, push, then STOP for Helder pattern audit before Waves D–U.** Docs-only work (.md + .sln) — NO worktree/subagent mandate; single agent, Sonnet. | 8229b0c | **Other sessions: do NOT hand-edit rows inside the `BACKLOG:GENERATED` fences — a hand-edit there is silently overwritten by `regen`, not merge-conflicted.** Change a row via its item `README.md` frontmatter or `backlog_gen.py status <ID>`; add items via `backlog_gen.py register`. Coordinate here first if you must touch a fenced row. Ownership ends when the branch merges. Write-ownership rule under definition: `spec-evolution-versioning/tasks.md` **T13d**; scoped deviation from "docs land on develop" authorized by Helder 2026-07-22 and recorded in `.claude/exception-registry.md`. |
| SC-ENH | Session Continuity enhancements (Phase 8, Tasks 11–15) | complete — verifier PASS; cleanup executed (30→8 worktrees, form-ux merged `f00543a`, 478/478) | develop (tooling scripts + docs only, no app code) | — | to review | 68e1852 | Helder: run two-terminal live demo (last open gate); optionally direct the 4 leftover agent worktrees |

## Completed (last 20)

| ID | Feature / Bug | Branch | Merged into develop | Commit |
|----|---------------|--------|---------------------|--------|

---

**Status vocabulary:** `dispatched` · `in progress` · `build failure` · `test failure` · `to review` · `blocked: <reason>` · `ready to merge` · `merged` · `abandoned: <reason>`

**Column notes:**
- **Branch** — the branch holding the changes (empty = work happened directly on develop, docs-only).
- **Worktree** — `.worktrees/<name>` path, or `—` if the worktree was already removed (branch still holds the commits).
- **Last commit** — short hash on the task branch, so a resuming session can `git log <hash>..<branch>` instantly.
- **Next action** — one line: what the next session should do (e.g. "fix failing test X", "await Helder review", "merge after wave 2").

**Resume chain (no globbing):** this ledger row → the feature's `task-log.md` `### Checkpoint` block (live step state, pinged on a ~10-min heartbeat) → read ONLY its Context manifest files. See `session-ops.md § Checkpoint Ping & Context Manifest`.
