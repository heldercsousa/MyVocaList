# Work Ledger — branch/status tracker (lives on develop)

> **Purpose:** single source of truth, always on `develop`, for WHERE every in-flight task/bug lives (branch, worktree) and its current phase/status. This is what makes resuming development, debugging, and merges into develop possible after any interruption. Maintained via `/sln-ledger` and at every phase transition (dispatch, phase change, merge, close). Rules: `workflow.md § Rule 2` (docs land on develop).
>
> **Row lifecycle:** add the row when work is dispatched to a branch/worktree; update `Phase/Step`, `Status`, and `Last commit` at each transition; on merge into develop set Status `merged` and move the row to the Completed section (keep last 20 for audit, prune older).

## Active

| ID | Feature / Bug | Phase / Step | Branch | Worktree | Status | Last commit | Next action |
|----|---------------|--------------|--------|----------|--------|-------------|-------------|
| DX-AC | Replace AutocompleteMobileField with DX AutoCompleteEdit | T2–T6 complete; merged to develop (verifier CONDITIONAL PASS, W1/W3 fixed, W2 = T7 watch item); 501/501 green; worktree removed; pushed | merged into develop | — | merged | e17cb4c | **Updated 2026-08-03 — items (2) and (3) are DONE, not pending.** T7 was run; it surfaced BUG-050, BUG-051 and BUG-052, all three since fixed and ✅ closed. The REQ-DXAC-03 blur conflict resolved in favour of retaining typed text (re-run #5 item D3 ✅), so **BUG-027 IS closed by this swap** — closed ✅ Fixed 2026-08. BACKLOG rows closed 2026-08-03. Remaining: confirm no residual DX-migration defect, then set this row ✅ and move it to Completed. |
| INLINE-AC | Song artist field — correctness fixes (BUG-050/051/052 + retain-text) + inline create-new-artist | T1–T9 done + BUG-053 fixed (`8d33547`); **T10 on-device FAILED 2026-07-22** — Part A (BUG-053) ✅; 6 defects remain → **BUG-054…059** (root-caused to DX AutoCompleteEdit wiring/XAML, file:line in task-log § T10 outcome). Fix wave done (code review CONDITIONAL PASS, 520/520). **T10 re-run #2 (2026-07-23): a/e/j/C1/C2 ✅; b/c/i FAIL + new defects.** New/reopened: BUG-060 (artist locks permanently), BUG-057 (reopened — error text invisible), BUG-059 (reopened — catalog empty, not a cascade), BUG-061 (lingering suggestion row); separate/governed: BUG-062/063 (Songs list). **Fix batch 2 (Sonnet, `b0e45da`, 523/523):** BUG-060 (ClearArtistCommand+ClearIconClicked), BUG-057 (ArtistErrorText never set — 1-line), BUG-061 (SelectedItem=null reset) ALL FIXED. **BUG-059 CANCELLED (Helder 2026-07-23 — works-as-designed):** Catalog join table is deliberately picker-only; empty catalog after Song-form save is by design. Reframed as a NEW enhancement (auto-link artist-OWNED songs to author's catalog, perpetual/unremovable hook; manual catalog = performers/covers only) — seed: `BusinessFeatures/artists-songs/ENHANCEMENT-artist-owned-song-catalog-autolink.md`; register in BACKLOG when SPEC-EVO migration settles. T10 item i to be dropped. So INLINE-AC's own defects are ALL fixed (060/057/061 + earlier wave); only Helder's on-device T10 re-run #3 (b/c/change-artist/lingering-row) gates closeout. ⚠️ Android build blocked locally by `XARLP7024` (AV/EDR file corruption on AndroidX AAR extract, NOT code — verified 0 errors on net10.0 TFM); Helder builds Android locally. **T10 re-run #3 (2026-07-25): item 1 (BUG-060 change-artist/unlock) ✅ PASSED-closed;** items 2&3 re-fixed this session — BUG-064 (`283866f`, duplicate error label removed) + BUG-061 completion (`7c594e2`→`3e38066`→`b8f7d2c`, `_suppressNextArtistSearch` guard on ALL 7 programmatic `ArtistSearchText` sites; +7 tests). Verifier CONDITIONAL PASS, no blockers, all residuals closed. **HEAD `b8f7d2c` PUSHED, 530/530 green.** **T10 re-run #4 (Helder, on-device, 2026-07-30) = FAILED.** BUG-061's core behavior ✅ (select→fill→hide→lock→clear→re-select overrides) and BUG-064 not recurring, but THREE new defects: **BUG-067 (Critical)** edit-mode artist change not persisted (song keeps original artist — silent loss of a user edit; regression test MANDATORY first); **BUG-066 (Major)** inline create-new-artist unreachable in BOTH modes ("search and select an artist from the list" on a novel name) — C1 passed in re-run #2, so this is a REGRESSION from a later fix wave (suspects: BUG-054a sentinel suppression, `_suppressNextArtistSearch`); **BUG-065 (Major)** spurious "Not found" row — (a) after every programmatic text assignment, clearing only on blur, (b) at 1 typed char even when matches exist, resolving at 2. Verbatim report + per-item table + hypotheses: task-log § T10 re-run #4. **BUG-065 diagnosis COMPLETE 2026-07-30 — root cause proven from IL, no code written (Helder stopped the session to resume later).** Decompiled DX Editors 25.2.4: `AsyncItemsSourceProvider.OnEditorTextChanged` early-returns on `e.Reason != UserInput`, so **`ItemsRequested` never fires for programmatic text changes** — `_suppressNextArtistSearch` (BUG-061's fix, 7 sites) is never consumed by its own assignment, survives, and is eaten by the user's NEXT KEYSTROKE, which returns empty and skips the ➕-append. That single mechanism explains **BUG-065(b) AND BUG-066**. Settled: `CharacterCountThreshold` is `>=`/correctly set (not the cause); "Not found" is DX's own localizer string with no bindable override; `IsDropDownOpen` is the supported way to close the popup; leaving `RequestAsync` unassigned CRASHES. BUG-067 not investigated at all. | `feat/inline-artist-create` | `../MyVocaList-inline-ac` | **in progress — fix wave dispatched 2026-08-02** (Helder's decision taken, `2aebac3`: D1 = delete the 7-site guard, use `IsDropDownOpen = false`; D2 = order 065+066 -> 067 -> single on-device re-run #5). Step 1 (BUG-065+066, one root cause) dispatched to an implementor; BUG-061's tests `SongFormViewModelTests.cs:476-535` are authorised for rewrite against the real mechanism. **Step 1 DONE `befc2fe` (pushed):** guard deleted at all 7 sites, replaced by two-way-bound `IsArtistDropDownOpen` -> `dxe:AutoCompleteEdit.IsDropDownOpen`; the 7 BUG-061 tests rewritten against the real mechanism. Orchestrator-verified independently: 530/530 pass / 0 fail on net10.0, only 2 residual guard mentions (both explanatory comments), HEAD == origin. **Step 2 (BUG-067, Critical, undiagnosed) dispatched 2026-08-02** — systematic-debugging + Red-before-Green mandated; may add an AC to `requirements.md` (SDD invariant). **Step 2 DONE `295e91a` (pushed) — BUG-067 root cause: `UpdateSongAsync` had NO `artistId` parameter at all; the FK was a create-path-only assignment, so the edit path was structurally incapable of saving an artist change.** Fixed in `Services/SongService.cs` (`int? artistId = null` = keep-existing; unknown-artist rejected before write; uniqueness checked against the effective artist); VM forwards `SelectedArtistId` (plumbing only). Red observed (4 failed/1 passed of 5) before Green. **REQ-ACREATE-16 added** to `requirements.md`. **Verifier: CONDITIONAL PASS, no blockers** — the 10 Moq edits confirmed pure arity propagation; all 7 dropdown sites covered; no accidental FK null-out at any call site. Its one finding (2 of the 7 rewritten BUG-061 tests asserted the property default without seeding it true) fixed in `e13a495`, proven by comment-out/restore. **Branch HEAD `e13a495`, pushed, 535/535 green (orchestrator-verified).** **T10 re-run #5 (Helder, device + emulator, 2026-08-02) = FAILED — branch must NOT be merged.** PASSED and closed: BUG-065(b), BUG-066 (➕ create works in BOTH modes), BUG-060, hydration, retain-text. THREE new defects: **BUG-068 (Critical)** — EF Core `InvalidOperationException` "another instance with the same key value {'Id'} is already being tracked" at `SongRepository.UpdateAsync` (`Infra\Repository\SongRepository.cs:135`) ← `SongService.UpdateSongAsync` (`Services\SongService.cs:156`); tapping a suggestion ONCE then saving reports **success but persists nothing** (silent data loss), tapping the re-shown row then saving throws. BUG-067's fix + REQ-ACREATE-16 are correct and stay — they were necessary, not sufficient. **BUG-069 (Major)** — dropdown *dismisses then re-opens* after a selection listing all prefix matches (and on edit-load); NOT the old BUG-061 lingering-row mechanism; it is the **trigger** for BUG-068's throwing path. **BUG-070 (Minor/UX)** — "Search and select an artist from the list" implies inline create is impossible; needs Helder's copy decision + likely an AC change. Unit suite missed all of it: `SongServiceTests` mock `ISongRepository` so `DbSet.Update` never runs, and `SongRepository` has no real-SQLite update-after-read test (`testing.md § anti-patterns` gap). **Helder's instruction 2026-08-02: SPLIT the fix across sessions — A = BUG-068 only (Red at the repository/real-SQLite seam FIRST), B = BUG-069 only (establish the re-open path from DX IL before editing), C = BUG-070 (spec-first, blocked on Helder). Do NOT chain them in one session** — wide sessions caused re-runs #3–#5 to fail. **T10 re-run #6 CANCELLED by Helder 2026-08-04** — no reason to device-test a stopgap that the unit-of-work refactor deletes. **Session A (BUG-068) DONE 2026-08-03:** registered as **BUG-071** (the allocator had already passed 068; both numbers name one folder), root-caused to an application-wide DI lifetime defect, fixed at the repository seam by `1a114c1` on `feat/inline-artist-create` (537/537 green, Red observed first at the real-SQLite seam). That fix is an explicit **STOPGAP** slated for deletion by the UOW item below. Face 1 (silent success, nothing persisted) was never observed fixed — now closed by **inference** (believed BUG-069), not evidence. **B (BUG-069) and C (BUG-070) remain unstarted.** ⚠️ **Unresolved for Helder:** commit `528ac31` on develop, titled `docs(inline-ac): …`, also swept in the **SPEC-EVO T13a–d rules bundle** (CLAUDE.md, workflow.md, bug-tracking.md, 6 library files, exception-registry, changelog) — which sits behind the `CLAUDE.md § Authorship` HARD gate. Already pushed; NOT rewritten (history rewrite on a shared pushed branch needs Helder's explicit call). | b8f7d2c (worktree clean, untouched) | **Next session — fix wave in the SAME worktree `C:\Users\helde\source\repos\myvocalist-inline-ac`, strictly sequential** (all three converge on `SongFormPage.xaml(.cs)` + `SongFormViewModel.cs`): **FIRST take the open architectural decision** recorded in `handoff.md` § STOP POINT (3 options; recommended = delete `_suppressNextArtistSearch` at all 7 sites and use `IsDropDownOpen = false`). Then fix 065+066 together (one root cause), then **BUG-067 (Critical, Red→Green first — still unanalysed)**, then T10 re-run #5 on device re-verifying BUG-061 + BUG-064. Read `task-log.md § IL evidence (2026-07-30)` before touching code — it closes several questions that must not be re-investigated — both likely in `OnArtistItemsRequested`. Incremental single-file XAML edits. Then **T10 re-run #5** on-device (Helder), re-verifying BUG-061 + BUG-064 in the same pass. Only on all-green: closeout in `changes/2026-07-21-inline-artist-create/pending-backlog-closeout.md`, merge+push, remove worktree, unblock catalog. ⚠️ The 3 new ids are task-log-tracked, NOT `backlog_gen.py register`-ed — the allocator would reissue BUG-053 (see SPEC-EVO POST-MIGRATION-FOLLOWUPS FUP-4). |
| ITF | Inline Trivial Fix lane — bounded orchestrator inline-edit exception | Rules amended + Guard 3 merged (verifier CONDITIONAL PASS, all findings resolved); worktree-base-check false-positive fix merged; 38/38 green on develop; worktrees removed | merged into develop | — | merged | a4c198a | Helder: observe the FIRST LIVE ITF FIX end-to-end (last open gate) — opportunistic, waits for a qualifying fix. Full resume instructions incl. how to recognise/run one: `DevCycleCraft/inline-trivial-fix/handoff.md`. Also awaiting a decision on the 4 misbased `agent-*` worktrees (1 unmerged commit each — listed in that handoff). |
| UOW | DbContext lifetime & unit-of-work pattern (MAUI has no per-page scope) | **Phases 0, 1 and 2 COMPLETE. STOP — Phase 3 is a HARD GATE; Phase 4+ must NOT be started.** Suite on `feat/uow-pilot` at `f6670092`: **Failed 0, Passed 564, Skipped 0, Total 564** (orchestrator-verified). Phase 0 baseline was Failed 3 / Passed 525 / Skipped 0 / Total 528 — the 3 failures were the INTENTIONAL RED tests (REQ-UOW-03, REQ-UOW-22/24-nested) and all three went RED -> GREEN in Phase 2, which is the pilot's evidence that the pattern actually fixes BUG-071 at the seam it was designed for. **Phase 0:** real-DI SQLite harness `UnitOfWorkTestHost` built (the project had ZERO service-over-real-DB tests — exactly why the unit suite missed BUG-068). REQ-UOW-04 premise FALSIFIED at execution (finding F8): Artist/Person/Venue cannot be made to fail — kept as characterization tests, Helder chose Option A. **Phase 1:** contracts + `PassthroughUnitOfWork` + `UnitOfWork.cs` (independently verified against Revision 12: `_ambientScope` assigned in exactly 2 places, both write paths; `ExecuteReadAsync` never publishes, never saves) + 4 nesting tests + save-skip signal detection with fail-closed on unrecognised `TResult` (`28c46112`) + the `MauiProgram`/`AddAppServices` registration swap to `AddDbContextFactory(..., ServiceLifetime.Scoped)` (`d0a22a41`). **Phase 2 — all 9 pilot methods wrapped**, in order: `64a16ebb` (2.1 ArtistService); `372d691d` (blocker fix — `IUnitOfWork` threaded into `AddAppServices`; retired the superseded legacy-host characterization test); `77fb5071` (2.2 SongService create/update); `5a6d7ad2` (2.2b `DeleteSongsAsync`); `6d5766a7` (REQ-UOW-35 `FlushAsync`); `51c851bf` (2.3 ArtistResolutionService + retired `IArtistRepository.SaveChangesAsync`); `2a98c59d` (docs tick); `3896d253` (2.4 SongResolutionService, 3-level join); `f6670092` (2.4b retired `ISongRepository.SaveChangesAsync`). **REQ-UOW-35 is a NEW acceptance criterion added mid-flight by Helder's decision:** `IUnitOfWork.FlushAsync`, because REQ-UOW-09's CreateNew branch needs the DB-generated key *inside* the unit of work, and REQ-UOW-09 explicitly permits "two saves inside one unit of work". **The spec was updated IN PLACE** (`requirements.md`/`design.md`) — legitimate, the feature has not shipped (`workflow.md § SDD Invariant`). **REQ-UOW-11 partially satisfied:** the `IArtistRepository`/`ISongRepository` save entry points are retired; 3 of 5 remain and are owned by Phase 4+. **REQ-UOW-18 is vacuous** — `git merge-base --is-ancestor 1a114c1 HEAD` exits 1, so the `1a114c1` stopgap is not on this branch and there is nothing to delete. Two defects registered from Phase 2: **BUG-075** (pre-commit hook inoperative in worktrees — `dotnet test` without `--no-restore` aborts on `NETSDK1147`, so the test gate silently does not run and every worktree commit needs `--no-verify` for a reason unrelated to red tests) and **BUG-076** (flaky `ObjectDisposedException: 'SQLitePCL.sqlite3'` during `EnsureCreated`, moving victim each run, passes in isolation — threatens Phase 3.1's "suite green" gate). **Still true and load-bearing:** (a) a green Phase 3 proves the PATTERN, **not** that BUG-071 is gone — Queue/Event keep the defect on the shared context until Phase 3.5; (b) do NOT touch any file carrying `TODO [BUG-071 / UOW]` (`git grep -n "TODO [BUG-071 / UOW]"`) — Queue/Event are out of scope by D12; (c) **REQ-UOW-28 is THE load-bearing rule** for every wrap — the lambda body resolves repositories and services from its OWN `IServiceProvider`, never from `_`-prefixed constructor-injected fields; violating it compiles, passes tests, and leaves the bug alive; (d) `IBaseRepository.SaveChangesAsync` must NOT be deleted wholesale — the remaining explicit calls are load-bearing and disappear naturally as methods get wrapped. Spec APPROVED by Helder 2026-08-04 after four review passes (all four found real blockers, all resolved); a 5th pass has NOT been run. | `feat/uow-pilot` | `C:\Users\helde\source\repos\myvocalist-uow` | **awaiting Helder — Phase 3 HARD GATE** | f6670092 (clean, pushed) | **RESUME HERE: Phase 3 VERIFY — a HARD GATE. Do NOT start Phase 4+.** Items **3.1, 3.2, 3.4, 3.5 can be prepared** without Helder. **3.3 CANNOT: it requires Helder's on-device confirmation on a COLD APP START with NO Queue and NO Event screen visited** (visiting either re-introduces BUG-071 on the shared context and invalidates the run). 3.1 is "suite green" — re-run it aware of **BUG-076**, whose flake can make a green run look red or a lucky run look like evidence. **Phase 3.5 (repository-family merge + Queue/Event UoW migration, combined) is still `💡 Pending` with NO spec written, and it BLOCKS Phase 4+.** Direction already verified: DELETE `Infra/Repositories/` (plural, 2 files, embedded saves), KEEP `Infra/Repository/` (singular, 10 files, stage-only). Two traps carried forward: `Event` exists in BOTH entity namespaces with different shapes; `QueueServiceNew`/`EventService` have ZERO `SaveChangesAsync` calls and rely entirely on the embedded saves — converting them without a commit boundary silently stops persistence. Docs for this item are synced to develop as of 2026-08-18; the branch carries code only. Do not merge `feat/uow-pilot` into develop — that is Helder's call at the gate. |
| SC-ENH | Session Continuity enhancements (Phase 8, Tasks 11–15) | complete — verifier PASS; cleanup executed (30→8 worktrees, form-ux merged `f00543a`, 478/478) | develop (tooling scripts + docs only, no app code) | — | to review | 68e1852 | Helder: run two-terminal live demo (last open gate); optionally direct the 4 leftover agent worktrees |

## Completed (last 20)

| ID | Feature / Bug | Branch | Merged into develop | Commit |
|----|---------------|--------|---------------------|--------|
| TRIM-PERSIST | String trimming on persistence — Task 6 EF Core `ValueConverter`s (Task 7 integration merge) | `feat/persisted-string-trimming-converters` | 2026-08-04 — merge `761a1f0f`; conflict-free. develop merged into the branch first (`d08636db`) so develop never held an unverified state. On develop after merge: build 0 errors, 519/519 green, 6/6 PersistedStringTrimmingTests (real SQLite). Task 6 had been complete but UNMERGED since 2026-07-19 — `TrimForStorage` had zero production call sites on develop until now. **Row stays 🟡 In Progress:** all tasks checked, but Helder's on-device E2E gate (REQ-TRIM-01/02) is unperformed. **Worktree `../MyVocaList-wt-trim-persist` still present — safe to remove.** | 01625df |
| SEARCHBAR | AppBar / SearchAppBar Interaction Redesign — persistent MD3 SearchBar in CrudListView (T1–T11) | `feature/persistent-searchbar` | 2026-08-03 — merge `9b462c4`; build 0 errors, 513/513 green. 4 conflicts resolved additively (changelog, .sln, task-log, generated BACKLOG). BACKLOG row ✅ Done. **Worktree `../MyVocaList-wt-searchbar` still present — safe to remove.** Carried forward, NOT part of this closure: BUG-048 (CrudListView pagination reload) + BUG-049 (VenueFormPage post-save nav), both pre-existing, neither registered via `backlog_gen.py`. | 997e784 |
| SPEC-EVO | Spec Evolution — nested `bugs/`/`changes/` folders + generated BACKLOG | merged into develop | 2026-08-02 — ✅ Done, closed 2026-08. Row moved here from Active per the lifecycle rule (merged ⇒ Completed). No open actions. FUP-1 (folder-collision inversion) remains deferred by Helder; the BUG-022 parent/path regen warning is cosmetic metadata on a closed bug. | 9f14807 |

---

**Status vocabulary:** `dispatched` · `in progress` · `build failure` · `test failure` · `to review` · `blocked: <reason>` · `ready to merge` · `merged` · `abandoned: <reason>`

**Column notes:**
- **Branch** — the branch holding the changes (empty = work happened directly on develop, docs-only).
- **Worktree** — `.worktrees/<name>` path, or `—` if the worktree was already removed (branch still holds the commits).
- **Last commit** — short hash on the task branch, so a resuming session can `git log <hash>..<branch>` instantly.
- **Next action** — one line: what the next session should do (e.g. "fix failing test X", "await Helder review", "merge after wave 2").

**Resume chain (no globbing):** this ledger row → the feature's `task-log.md` `### Checkpoint` block (live step state, pinged on a ~10-min heartbeat) → read ONLY its Context manifest files. See `session-ops.md § Checkpoint Ping & Context Manifest`.
