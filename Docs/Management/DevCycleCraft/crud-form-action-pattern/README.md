---
id: crud-form-action-pattern
title: "**Song form — stays full-screen page + AppBar-save pattern**"
status: "✅ Done"
target: 2026-07-11
section: BusinessFeatures
kind: change
closed: 2026-07
order: 50
goal: "keep Song form as a full-screen page and move Save to the AppBar trailing slot, implemented 2026-07-12 (Helder-authorized sequencing override)."
pointer: DevCycleCraft/crud-form-action-pattern/
---

# Song form — stays full-screen page + AppBar-save pattern

Song form remains a full-screen page (not a BottomSheet); Save moved to the AppBar trailing
slot per the MD3 CRUD Form Action Pattern. Implemented 2026-07-12 under a Helder-authorized
sequencing override.

> Migrated from the 2026-07 archive row (T12a Wave O). This is the Business-table row
> (`Song form → stays full-screen page + AppBar-save pattern`); the id `crud-form-action-pattern`
> is the **primary** README for this folder (Venues precedent, F-5), filed top-level with no
> `parent` since the folder is not physically nested under `artists-songs`. The distinct
> Craft-table row ("CRUD Form Action Pattern — MD3 Save/Cancel placement") is filed as a
> separate `changes/` sub-item under this same folder with its own id and order — see
> Blocker #2 ruling, not merged into this row. Folder pre-existed (`design.md`, `plan.md`,
> `requirements.md`, `task-log.md`); only this `README.md` is new.
