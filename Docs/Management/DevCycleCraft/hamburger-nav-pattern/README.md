---
id: hamburger-nav-pattern
title: "**Hamburger menu on all hamburger-loaded pages (CRUD-only scope)**"
status: "✅ Done"
target: 2026-07-11
section: BusinessFeatures
kind: change
closed: 2026-07
order: 60
goal: "hamburger menu on every hamburger-loaded page, shipped for the 4 CRUD list pages; Shell-native pages deferred to the AppBar/SearchAppBar redesign."
pointer: DevCycleCraft/hamburger-nav-pattern/
---

# Hamburger menu on all hamburger-loaded pages (CRUD-only scope)

Shipped for the 4 CRUD list pages. Shell-native pages are deferred to the
AppBar/SearchAppBar redesign.

> Migrated from the 2026-07 archive row (T12a Wave O). Section kept as `BusinessFeatures`
> (this is a Business-table row) with no `parent` — the folder is not physically nested
> under a single Business feature, it spans all 4 CRUD list pages. Folder pre-existed
> (`design.md`, `findings.md`, `plan.md`, `requirements.md`, `task-log.md`, `tasks.md`);
> only this `README.md` is new.
